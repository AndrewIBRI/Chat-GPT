| eval volume=if(max>=1000000, tostring(round(max/1000000,1))."M",
            if(max>=1000, tostring(round(max/1000,1))."K",
            if(min>=1000, tostring(round(min/1000,1))."K",
            tostring(min)." - ".tostring(max))))
