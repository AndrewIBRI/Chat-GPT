| eval volume=if(volume=="0-500000", "0-500k", 
              if(max>=1000000, "1M+", 
              if(max>=1000, tostring(round(max/1000))."k-".tostring(round(max/1000)+500)."k", 
              if(min>=1, tostring(round(min,1))."K-".tostring(round(min+0.5,1))."K", volume))))
